export enum PRISMA_MEMBER_ROLE {
  OWNER = 'OWNER',
  OPERATOR = 'OPERATOR',
  DEVELOPER = 'DEVELOPER',
}

export enum SYS_PAGE_ROLE {
  PUBLIC,
  ADMIN,
}

export enum CallbackStatus {
  PENDING = 'PENDING',
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}

export enum SERVER_TYPE {
  TEST = 'TEST',
  LIVE = 'LIVE',
}
