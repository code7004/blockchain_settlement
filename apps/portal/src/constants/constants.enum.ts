export enum SYS_PAGE_ROLE {
  PUBLIC,
  ADMIN,
}

export enum SERVER_TYPE {
  TEST = 'TEST',
  LIVE = 'LIVE',
}

export enum AGGrid_Theme_TYPE {
  Quartz = 'Quartz',
  Balham = 'Balham',
  Material = 'Material',
  Alpine = 'Alpine',
}
